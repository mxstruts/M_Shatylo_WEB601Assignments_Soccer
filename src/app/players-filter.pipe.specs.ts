import { PlayersFilterPipe } from './players-filter.pipe;

describe('PlayersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});